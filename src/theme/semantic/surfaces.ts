import { semanticColors } from './colors';

export const surfaces = {
  page: semanticColors.background.page,
  layout: semanticColors.background.layout,
  container: semanticColors.background.container,
  elevated: semanticColors.background.elevated,
  subtle: semanticColors.background.subtle,
  selected: semanticColors.background.selected,
  mask: semanticColors.background.mask,
} as const;
