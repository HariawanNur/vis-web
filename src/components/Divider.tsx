import React from "react"
import { Divider as AntDivider, DividerProps as AntDividerProps } from "antd"

export const Divider: React.FC<AntDividerProps> = (props) => <AntDivider {...props} />

export default Divider
