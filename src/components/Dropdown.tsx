import React from "react"
import { Dropdown as AntDropdown, DropdownProps as AntDropdownProps } from "antd"

export const Dropdown: React.FC<AntDropdownProps> = (props) => <AntDropdown {...props} />
export default Dropdown
