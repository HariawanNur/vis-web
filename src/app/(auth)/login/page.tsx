import type { Metadata } from "next"

import Page from "./client"

export const metadata: Metadata = {
  title: "Login",
  description: "Login untuk mengakses platform admin Vistara",
}

export default Page
